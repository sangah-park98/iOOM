package egovframework.pf.member.interceptor;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;

import org.springframework.web.servlet.HandlerInterceptor;
import org.springframework.web.servlet.ModelAndView;

/*public class LoginInterceptor implements HandlerInterceptor {

  @Override
    public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) throws Exception {
        String uri = request.getRequestURI();
        System.out.println("1111");
        if (uri.equals("/") || uri.equals("/code/commonCodeList") || uri.equals("/login") 		   || uri.equals("/member/pwSendEmail")
                || uri.equals("/member/searchIdAndPw")  || uri.equals("/member/idCheck") 		   || uri.equals("/member/emailCheck")
                || uri.equals("/member/certNumberSend") || uri.equals("/member/certNumberConfirm") || uri.equals("/member/certTimeout")
                || uri.equals("/member/companyCheck")   || uri.equals("/reportDown/downList")      || uri.equals("/member/memberCheck")   
                || uri.equals("/member/idSendEmail") 	
            )
        	
        	
        {
            return true;
        }
        HttpSession session = request.getSession();
        if (session.getAttribute("USER") == null) {
            String javascript = "loginfalse";
            response.getWriter().print(javascript); 
            response.getWriter().flush();
            return false;
        } else{
            return true;
        }
        
    }

    @Override
    public void postHandle(HttpServletRequest request, HttpServletResponse response, Object handler, ModelAndView modelAndView) throws Exception {
    		
    }

    @Override
    public void afterCompletion(HttpServletRequest request, HttpServletResponse response, Object handler, Exception ex) throws Exception {

    }
}*/
